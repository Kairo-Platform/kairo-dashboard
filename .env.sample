# Environment variables

Each app manages its own environment. Copy the relevant `.env.sample` to `.env.local`:

- Admin: `apps/kairo-admin-dashboard/.env.sample`
- Enterprise: `apps/kairo-enterprise-dashboard/.env.sample`
